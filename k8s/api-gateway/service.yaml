apiVersion: v1
kind: Service

metadata:

  name: api-gateway
  namespace: distributed-url-shortener

  labels:
    app: api-gateway

spec:

  selector:
    app: api-gateway

  ports:

    - port: 8081
      targetPort: 8081

  type: ClusterIP